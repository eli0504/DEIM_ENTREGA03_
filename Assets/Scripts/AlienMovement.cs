using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AlienMovement : MonoBehaviour
{
    /*private Vector2Int startPostion;

    //GRID
    public int row = 5;
    public int column = 11;

    //ALIEN
    public GameObject enemyPrefab;

    //VARIABLE FOR THE ENEMY TO MOVE EVERY 1 SECOND
    private float gridMoveTimer;
    private float gridMoveTimerMax = 1f;

    //Two-dimensional array to be able to instantiate copies of a single gameobject
    public GameObject[,] enemyArray;

    // Update is called once per frame
    void Update()
    {
       
    }


    /*private void Start()
    {
        //THE G.O GOES THROUGH ALL THE POSITIONS
        enemyArray = new GameObject[column, row];

        for (int i = 0; i < column; i++)
        {

            for (int j = 0; j < row; j++)
            {

                enemyArray[i, j] = (GameObject)Instantiate(enemyPrefab, new Vector3(i, j, 0), Quaternion.identity);

            }
        }

    }*/
}
